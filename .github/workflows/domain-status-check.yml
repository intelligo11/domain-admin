name: Domain Status Check

# 定时任务：每天北京时间上午 9:30 执行
on:
  # schedule:
  #   - cron: '30 1 * * *'  # UTC 1:30 = 北京时间 9:30
  workflow_dispatch:     # 支持手动触发

jobs:
  check-status:
    runs-on: ubuntu-latest
    steps:
      - name: Validate Configuration
        run: |
          if [ -z "${{ secrets.PAGES_URL }}" ]; then
            echo "❌ 错误：PAGES_URL Secret 未配置！"
            echo "请在仓库设置中添加 PAGES_URL Secret"
            echo "路径：Settings > Secrets and variables > Actions > New repository secret"
            exit 1
          fi
          echo "✅ PAGES_URL 已配置"
          
      - name: Check Domain Status
        run: |
          # 🔒 使用 API Token 认证保护接口
          echo "🚀 开始执行域名状态检查任务..."
          
          # 构建带 Token 的 API URL
          API_URL="${{ secrets.PAGES_URL }}/api/cron/check-status"
          if [ -n "${{ secrets.CRON_API_KEY }}" ]; then
            API_URL="${API_URL}?token=${{ secrets.CRON_API_KEY }}"
          fi
          
          response=$(curl -s -w "\n%{http_code}" -X GET "$API_URL")
          http_code=$(echo "$response" | tail -n1)
          
          echo "📊 HTTP Status: $http_code"
          
          if [ "$http_code" -ne 200 ]; then
            echo "❌ 状态检查失败 (HTTP $http_code)"
            echo "💡 提示：请检查 Cloudflare Pages 部署状态和 CRON_API_KEY 配置"
            exit 1
          else
            echo "✅ 状态检查完成"
          fi
